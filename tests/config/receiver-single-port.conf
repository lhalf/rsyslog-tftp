module(load="improg")
input(type="improg"
      binary="/usr/libexec/rsyslog/imtftp -port 1069 -single-port"
      tag="tftp")

template(name="received" type="string" string="%msg%\n")
action(type="omfile" file="/var/log/received.log" template="received")
