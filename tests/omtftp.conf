module(load="imtcp")
input(type="imtcp" port="10514")

module(load="omprog")
action(type="omprog"
       useTransactions="on"
       binary="/usr/bin/omtftp tftp://imtftp:1069/irrelevant"
       output="/dev/stdout")

action(type="omfile" file="/dev/stdout")
